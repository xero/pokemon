# The card-data cache normalize_cards.py keeps: one raw TCGplayer record per
# product id. It rebuilds itself from product-ids.tsv on the next run, so it
# is never worth committing.
raw-cards.json

# Scratch
*.bak
.DS_Store
__pycache__/
